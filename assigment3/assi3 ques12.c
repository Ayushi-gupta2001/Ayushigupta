#include<stdio.h>
void main()
{
    int a,b,c,p,q,r;
    int distance,radius=r,area;
    printf("enter value of p,q,r,a,b,c");
    scanf("%d%d%d%d%d%d",&p,&q,&r,&a,&b,&c);
    if(distance<radius)
         printf("circle and line will intersect %d");
       else
          printf("circle and line will not intersect %d");
}









